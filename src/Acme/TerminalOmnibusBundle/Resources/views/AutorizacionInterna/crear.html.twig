{% extends "::content.html.twig" %}

{% block contentHeaderModal %}
    Crear Autorización Interna
{% endblock %}
    
{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function(){
            crearAutorizacionInterna.funcionesAddOnload();	
        }); 
    </script>
{% endblock %}
           
{% block contentBodyModal %}
    
    <form class="form-horizontal autorizacionInternaForm" id="autorizacionInternaForm" name="autorizacionInternaForm" action="{{ path('autorizacionInterna-crear-case1') }}" method="post" {{ form_enctype(form) }}>
       
        <input type="hidden" name="mensajeServidor" id="mensajeServidor" value="{{ mensajeServidor }}"></input>
        {{ form_errors(form) }}
        
        <div class="row-fluid"> 
            <div class="span12">
                {{ form_label(form.motivo) }}
                {{ form_widget(form.motivo , {'attr': {'placeholder': 'Motivo de la creación de la autorización interna.' } }) }}
                {{ form_errors(form.motivo ) }}
            </div>
        </div> 
        
         <div class="row-fluid"> 
            <div class="span12">
                {{ form_label(form.codigo) }}
                {{ form_widget(form.codigo , {'attr': {'placeholder': 'PIN de la autorización interna.' } }) }}
                {{ form_errors(form.codigo ) }}
            </div>
        </div> 
        
        <input type="hidden" id="pathGeneratePin" value="{{ url('ajaxGeneratePin') }}"></input>
        <input type="hidden" id="pathHomeAutorizacionInterna" value="{{ url('autorizacionInterna-home') }}"></input>
        {{ form_rest(form) }}
        
    </form>
    
{% endblock %}

{% block contentFooterModal %}
   
    <a id="aceptar" href="#" class="btn btn-primary">Aceptar</a>
    <a id="cancelar" href="{{ url('homeInternal-default') }}" class="btn">Cancelar</a>
     
{% endblock %}






