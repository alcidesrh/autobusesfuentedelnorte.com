<form action="{{ path(route) }}" method="post" {{ form_enctype(form) }}>
    
    <input type="hidden" name="mensajeServidor" id="mensajeServidor" value="{{ mensajeServidor }}"></input>
    {{ form_errors(form) }}
    
    <div class="container-fluid">
    <div class="row-fluid">
    <div class="span12">  
        <label> <strong> ¿Está seguro que desea acreditarle el depósito a la agencia {{ estacion.nombre }}, con {{ moneda.sigla ~ ' ' ~ saldo }} de saldo principal y {{ moneda.sigla ~ ' ' ~ bono }} de bono.</strong> </label>
    </div> 
    </div>
    </div> 
    
    {{ form_rest(form) }}
    
</form>